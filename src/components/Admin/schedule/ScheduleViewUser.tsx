import { UserSchedule } from "../../../types"

interface IScheduleViewUser {
    usersChedule: UserSchedule[]
}

export const ScheduleViewUser = ({ usersChedule }: IScheduleViewUser) => {
    return (
        <>

        </>
    )
}