import { Schedule } from "../../../types"

interface IScheduleView {
    schedule: Schedule
}
export const ScheduleView = ({ schedule }: IScheduleView) => {
    return (
        <>

        </>
    )
}