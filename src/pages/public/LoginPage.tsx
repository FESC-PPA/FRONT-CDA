import {LoginForm} from "../../components/";
import {AppLayout} from "../../layouts/AppLayout";

const LoginPage = () => {
  return (
    <AppLayout>
          <LoginForm></LoginForm>
    </AppLayout>
  );
};

export default LoginPage;
