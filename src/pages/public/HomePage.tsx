import { AppLayout } from "../../layouts/AppLayout";
import { AppHome } from "../../components";

const HomePage = () => (
  <AppLayout>
    <AppHome></AppHome>
  </AppLayout>
);

export default HomePage;
